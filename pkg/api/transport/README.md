Proposal. These are not yet implemented but are being considered as a means to automatically detect if a transport is
implemented so that an endpoint can automatically transform requests and responses.
