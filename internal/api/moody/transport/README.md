# `transport` package

Each folder/package of the `transport` package implements the server for the transport.

```text
{{.Transport}}
├── instance.go
├── listen.go
└── {{.Service}}
    └── server.go
```
