This branch begins with an existing [gRPC service and its generated Go source](../api/moody), initialized using the [`generate` Makefile target](../Makefile). It is also an initialzied Go module. Otherwise, it is almost entirely empty files.

Ensuring the tools required to generate the gRPC probobuf definitions is not covered but the versions are in [go.mod](../go.mod).
